{% from "components/imageBlock.njk" import ImageBlock %}
{% from "components/textBlock.njk" import TextBlock %}

{% macro IntroSection(props) %}

{# Sections are what we would turn into Block List items for Umbraco editors.
   Sections include components #}

  <section class="section intro">
    <div class="container">
      <div class="intro-grid">
        <div class="intro__heading">
          <h1>{{ props.title }}</h1>
        </div>
        <div class="intro__column">

          {{ TextBlock({
	          text: props.text
	        }) }}

        </div>
        <div class="intro__column">

          {{ ImageBlock({
	          image: props.image 
	        }) }}

        </div>
      </div>
    </div>
  </section>

{% endmacro %}
